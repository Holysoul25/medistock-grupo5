# Servidor
PORT=3000
NODE_ENV=development

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=medistock

# JWT
JWT_SECRET=medistock_dev_secret_2025
JWT_EXPIRES_IN=8h

# Frontend URL (para returnUrl de Webpay)
FRONTEND_URL=http://localhost:4200

# Webpay / Transbank
# En desarrollo usa el sandbox automáticamente (credenciales públicas de integración)
# En producción cambiar a credenciales reales y NODE_ENV=production

# Bluexpress
# BLUEXPRESS_MOCK=false        <- descomentar cuando tengan credenciales reales
# BLUEXPRESS_URL=https://api.bluexpress.cl/v1
# BLUEXPRESS_API_KEY=tu_api_key_aqui
# BLUEXPRESS_CUENTA=tu_cuenta_aqui
BLUEXPRESS_MOCK=true
